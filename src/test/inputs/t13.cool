class Main {
    let a : int[];
    let b : int[];
    let c: bool;
    let d: int;
    main() : int {
        a <- new Array (int,5);
        b <- new Array (int,3);
        a[0] <- 4;
        a[1] <- 2;
        a[2] <- a[1];
        a[2] <- a[1]+ a[0];
        c <- a==b;
        d <- len(a);
        d <- len(new Array (int,9));
        return 2;
    }
}
