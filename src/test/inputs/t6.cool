class Main {
    let a : bool;
    let b : bool;
    let c : bool;
    let d : int; 
    main() : int {
        a <- true;
        b <- false;
        c <- true && true;
        c <- a && b;
        c <- true || true;
        c <- a || b;
        b <- a && (true && b);
        b <- a && (a && b);
        a <- true;
        b <- false;
        c <- ! a;
        d <- 5;
        c <- (bool) 4;
        c <- (bool) d;
        c <- (bool) (d+6);
        return 2;
    }
}
